---
import Layout from "../layouts/BaseLayout.astro";
import testimonials from "../../data/testimonials.json";
---

<Layout
  title="Maatwerk op Wielen – Persoonlijke automakelaar"
  description="Uw persoonlijke automakelaar – van zoektocht tot aflevering."
>
  <!-- HERO -->
  <section
    id="hero"
    class="relative min-h-[85vh] flex items-center justify-center"
  >
    <!-- background image -->
    <img
      src="/auto2-edit1.jpg"
      alt="Moderne auto rijdt door zonnige straat"
      class="absolute inset-0 w-full h-full object-cover"
      loading="lazy"
    />
    <!-- dark overlay -->
    <div
      class="absolute inset-0 bg-gradient-to-b from-black/60 via-black/40 to-black/25"
    >
    </div>

    <div class="relative z-10 text-center px-4">
      <h1
        class="text-snow-gray font-serif text-4xl md:text-6xl leading-tight font-bold mb-4"
      >
        Maatwerk op wielen,<br class="hidden sm:inline" /> zonder gedoe.
      </h1>
      <p class="text-snow-gray/90 text-lg md:text-2xl mb-8">
        Uw persoonlijke automakelaar – van zoektocht tot aflevering.
      </p>

      <a
        href="#contact"
        class="inline-flex items-center gap-2 bg-coral py-3 px-8 rounded-lg text-white text-lg font-medium shadow-lg transition hover:-translate-y-0.5 hover:shadow-xl focus:ring-4 focus:ring-coral/40"
      >
        <i class="fa-regular fa-calendar-check"></i> Plan gratis intake
      </a>
    </div>
  </section>

  <!-- USP GRID -->
  <section id="usp" class="py-24 bg-snow-gray">
    <div class="container mx-auto px-4">
      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
        {
          [
            {
              icon: "shield-check",
              title: "100% onafhankelijk",
              text: "Geen voorraad, geen verkooppraatjes – alleen uw belang.",
            },
            {
              icon: "key",
              title: "Top-aanbod op maat",
              text: "Dealers, veilingen en particulieren in heel Nederland.",
            },
            {
              icon: "handshake",
              title: "Onderhandelen & check",
              text: "U bespaart tijd en geld; elke auto wordt grondig gecontroleerd.",
            },
            {
              icon: "user-check",
              title: "Eén aanspreekpunt",
              text: "Persoonlijk contact – reactie op WhatsApp binnen 24 uur.",
            },
          ].map(({ icon, title, text }) => (
            <div class="group p-6 bg-white rounded-2xl shadow-md transition hover:shadow-lg hover:-translate-y-1">
              <i
                class={`fa-solid fa-${icon} text-coral text-3xl mb-4 transition group-hover:scale-110`}
              />
              <h3 class="text-trust-blue font-semibold mb-2">{title}</h3>
              <p class="text-charcoal/90 text-sm leading-relaxed">{text}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- PROCES -->
  <section id="proces" class="py-28 bg-white">
    <div class="container mx-auto px-4">
      <h2
        class="text-center text-trust-blue font-serif text-3xl md:text-4xl font-bold mb-16"
      >
        Zo werkt het
      </h2>

      <div class="grid gap-12 sm:grid-cols-2 lg:grid-cols-4">
        {
          [
            {
              icon: "comments",
              step: "1. Intake",
              desc: "Uw wensen, budget en planning helder in kaart.",
            },
            {
              icon: "search",
              step: "2. Zoeken",
              desc: "Selectie van geschikte auto’s uit vertrouwde bronnen.",
            },
            {
              icon: "clipboard-check",
              step: "3. Controle & deal",
              desc: "Historie-check, proefrit en scherpe onderhandeling.",
            },
            {
              icon: "car-side",
              step: "4. Levering",
              desc: "Bij u thuis of op locatie – rijklaar en zonder zorgen.",
            },
          ].map(({ icon, step, desc }) => (
            <div class="text-center flex flex-col items-center">
              <i class={`fa-solid fa-${icon} text-coral text-4xl mb-4`} />
              <h3 class="text-trust-blue font-semibold mb-1">{step}</h3>
              <p class="text-charcoal/90 text-sm">{desc}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- OVER JASPER -->
  <section id="over" class="py-28 bg-snow-gray">
    <div class="container mx-auto px-4 grid gap-12 md:grid-cols-2 items-center">
      <img
        src="/owner.jpg"
        alt="Portret van Jasper Zweers"
        class="w-full h-auto rounded-2xl shadow-lg"
        loading="lazy"
      />

      <div>
        <h2
          class="text-trust-blue font-serif text-3xl md:text-4xl font-bold mb-6"
        >
          Over Jasper
        </h2>
        <p class="text-charcoal mb-4 leading-relaxed">
          <strong>Jasper Zweers</strong> – automakelaar met passie voor auto’s én
          service. Hij helpt particulieren en ondernemers bij het vinden, aankopen
          en afleveren van de juiste auto.
        </p>
        <p class="text-charcoal mb-4 leading-relaxed">
          U vertelt wat u zoekt, Jasper regelt de rest: zoeken, vergelijken,
          onderhandelen en leveren. Zo bespaart u tijd, geld én frustratie.
        </p>
        <p class="text-trust-blue italic font-medium">
          “Uw auto, uw wensen – mijn werk.”
        </p>
      </div>
    </div>
  </section>

  <!-- REVIEWS -->
  <section id="reviews" class="py-28 bg-white">
    <div class="container mx-auto px-4 text-center">
      <h2
        class="text-trust-blue font-serif text-3xl md:text-4xl font-bold mb-16"
      >
        Ervaringen van klanten
      </h2>

      <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-4">
        {
          testimonials.map(({ name, quote }) => (
            <figure class="relative bg-snow-gray p-6 rounded-2xl shadow-md">
              <img
                src={`https://source.unsplash.com/64x64/?portrait&sig=${name}`}
                alt={name}
                class="w-12 h-12 rounded-full object-cover absolute -top-6 left-1/2 -translate-x-1/2 border-4 border-white shadow"
                loading="lazy"
              />
              <blockquote class="mt-6 text-sm text-charcoal mb-4 leading-relaxed">
                “{quote}”
              </blockquote>
              <figcaption class="text-trust-blue font-semibold text-xs">
                — {name}
              </figcaption>
            </figure>
          ))
        }
      </div>
    </div>
  </section>

  <!-- TARIEF & FAQ -->
  <section id="tarief" class="py-28 bg-snow-gray">
    <div class="container mx-auto px-4">
      <h2
        class="text-center text-trust-blue font-serif text-3xl md:text-4xl font-bold mb-10"
      >
        Transparant tarief
      </h2>

      <div class="max-w-3xl mx-auto bg-white p-10 rounded-2xl shadow-md">
        <p class="text-center text-charcoal text-lg mb-8 leading-relaxed">
          Volledige begeleiding vanaf
          <span class="text-trust-blue font-bold">€ 595 incl. btw</span>. U
          betaalt pas wanneer we dé auto hebben gevonden.
        </p>

        {
          [
            {
              q: "Wat krijg ik voor dit bedrag?",
              a: "Persoonlijk advies, selectie uit betrouwbaar aanbod, historie- en technische check, onderhandeling en levering.",
            },
            {
              q: "Is het intakegesprek gratis?",
              a: "Ja, het intakegesprek is kosteloos en vrijblijvend.",
            },
            {
              q: "Hoe snel reageer je?",
              a: "Jasper reageert op WhatsApp doorgaans binnen 24 uur, ook in het weekend.",
            },
          ].map(({ q, a }) => (
            <details class="border-t border-gray-200 py-4 last:pb-0">
              <summary class="cursor-pointer font-semibold text-trust-blue leading-relaxed">
                {q}
              </summary>
              <p class="mt-4 text-charcoal text-sm leading-relaxed">{a}</p>
            </details>
          ))
        }
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact" class="py-28 bg-white">
    <div class="container mx-auto px-4 text-center max-w-xl">
      <h2
        class="text-trust-blue font-serif text-3xl md:text-4xl font-bold mb-6"
      >
        Plan uw gratis intake
      </h2>
      <p class="text-charcoal mb-10 leading-relaxed">
        Bel, mail of stuur een WhatsApp-bericht en Jasper neemt binnen 24 uur
        contact met u op.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="https://wa.me/316279665314"
          class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-coral text-white font-medium rounded-lg shadow-lg transition hover:-translate-y-0.5 hover:shadow-xl focus:ring-4 focus:ring-coral/40"
        >
          <i class="fa-brands fa-whatsapp"></i> WhatsApp
        </a>

        <a
          href="tel:+316279665314"
          class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-trust-blue text-white font-medium rounded-lg shadow-lg transition hover:-translate-y-0.5 hover:shadow-xl focus:ring-4 focus:ring-trust-blue/40"
        >
          <i class="fa-solid fa-phone"></i> Bel nu
        </a>

        <a
          href="mailto:jasper@maatwerk-op-wielen.nl"
          class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-honey-gold text-white font-medium rounded-lg shadow-lg transition hover:-translate-y-0.5 hover:shadow-xl focus:ring-4 focus:ring-honey-gold/40"
        >
          <i class="fa-regular fa-envelope"></i> E-mail
        </a>
      </div>
    </div>
  </section>
</Layout>
