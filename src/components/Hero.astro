---
// src/components/Hero.astro
const phone = "06 2796 65314";
---

<section id="hero" class="relative isolate overflow-hidden bg-white">
  <div class="container mx-auto px-6 py-32 text-center">
    <h1 class="text-4xl md:text-6xl font-extrabold leading-tight">
      Onafhankelijke <span class="text-primary">auto-makelaar</span> zonder gedoe
    </h1>
    <p class="mt-6 max-w-3xl mx-auto text-lg text-gray-700">
      Bespaar tijd, geld en zorgen bij het kopen <em>of</em> verkopen van uw auto.
      Eén contactpersoon, helder tarief.
    </p>
    <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
      <a
        href="#services"
        class="px-8 py-4 rounded-full bg-primary text-white hover:bg-red-700 transition"
        >Bekijk mijn diensten</a
      >
      <a
        href={`tel:+31${phone.replace(/\D/g, "")}`}
        class="px-8 py-4 rounded-full bg-gray-200 hover:bg-gray-300 transition"
        >Bel {phone}</a
      >
    </div>
  </div>
</section>
