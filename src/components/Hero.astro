---
// src/components/Hero.astro
const phone = "06 2796 65314";
const photo = "/owner.jpg";
---

<section id="hero" class="relative isolate overflow-hidden bg-gradient-to-b from-gray-50 to-white">
  <div class="container mx-auto px-6 py-20 md:py-32">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="order-2 md:order-1 text-left space-y-6">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
          Uw <span class="text-primary">persoonlijke automakelaar</span> – zonder gedoe
        </h1>
        <p class="text-lg text-secondary-light max-w-2xl">
          Hallo, ik ben <span class="font-semibold text-secondary">Jasper Zweers</span>. Ik help u bij het vinden, aankopen en afleveren van de juiste auto. Geen verkooppraatjes, maar een persoonlijke aanpak waarbij uw wensen voorop staan.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <a
            href="#services"
            class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-primary text-white hover:bg-primary-dark transition-colors duration-300 font-semibold shadow"
          >
            Bekijk mijn diensten
          </a>
          <a
            href={`tel:+31${phone.replace(/\D/g, "")}`}
            class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-primary text-white hover:bg-primary-dark transition-colors duration-300 font-semibold shadow border-2 border-primary"
            style="box-shadow: 0 4px 24px rgba(30,64,175,0.12);"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h2a2 2 0 012 2v10a2 2 0 01-2 2H3m0-14h18a2 2 0 012 2v10a2 2 0 01-2 2H3m0-14v14"/></svg>
            Bel {phone}
          </a>
        </div>
      </div>
      <div class="order-1 md:order-2 flex justify-center">
        <img
          src={photo}
          alt="Jasper Zweers automakelaar"
          class="rounded-2xl shadow-xl w-64 h-64 object-cover border-4 border-primary"
        />
      </div>
    </div>
  </div>
</section>
